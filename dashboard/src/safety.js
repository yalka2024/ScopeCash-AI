/**
 * Generated safety status for ScopeCash AI.
 *
 * When `gated` is true, this platform targets a safety-critical / regulated
 * domain and its domain/compliance logic is unvalidated scaffolding. The
 * SafetyBanner renders a non-dismissable warning so no one mistakes it for a
 * production-ready, validated product.
 */
export const SAFETY = {
  gated: false,
  category: '',
};

